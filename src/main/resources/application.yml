server:
  port: 9090

spring:
  h2:
    console:
      enabled: true

keycloak:
  realm: waisl
  auth-server-url: http://keycloak:8080/
  resource: springboot-keycloak
  public-client: true
  bearer-only: true